# Cài đặt PostgreSQL Dependencies

```bash
npm install pg @types/pg
npm install prisma @prisma/client
npm install -D prisma
```

Hoặc nếu dùng yarn:
```bash
yarn add pg @types/pg prisma @prisma/client
yarn add -D prisma
```